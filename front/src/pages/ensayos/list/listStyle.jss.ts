import { createUseStyles } from 'react-jss'
export const ListaEnsayosStyle = createUseStyles({

    list: {
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
        gap: 30,
        marginTop: 30,
        height: "calc(90vh - 1rem)",
        overflow: "auto"
    },

  
})