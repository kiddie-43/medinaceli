import { createUseStyles } from 'react-jss'
export const RehearsalFormStyle = createUseStyles({
    form: {
        width: "100%",
        display: "flex",
        flexDirection:"column", 
        gap:30
    },
    rowForm: {
        display:"flex", 
        gap:15
    }


})